# Отчёт вне нумерации 25: исключение NO_PROXY заведено

Дата: 05.08.2026. Поручение владельца: завести переменную `NO_PROXY` командой —
единственная разрешённая правка настроек машины. Условия: только для текущего
пользователя, только `NO_PROXY`, существующие переменные прокси не трогать,
перед правкой переписать нынешнее состояние в надёжное место.

**Обезличено.** Ни пароля, ни адреса прокси здесь нет.

## Сделано, в порядке исполнения

**1. Снимок состояния — до правки.** Обе нынешние переменные переписаны целиком
(вместе с паролем — иначе снимок не был бы возвратом) в файл:

    C:\Users\Andrey\Documents\proxy-peremennye-do-NO_PROXY-2026-08-05.txt

В файле же — одна строка, которой всё возвращается как было. Проверено: файл
записан, обе переменные в нём есть. В хранилище кода и в ящик отправки он не
попадает: лежит только на машине, в «Документах».

**2. Правка — одна, та самая.** Заведена переменная пользователя:

    NO_PROXY = .google.com,.googleapis.com,.googleusercontent.com,.gstatic.com

Проверено чтением из реестра после записи: переменная стоит, `HTTP_PROXY` и
`HTTPS_PROXY` не тронуты — обе на месте, значения прежние.

**3. Проверка механизма — пробой, до всякого Диска.** Тем же средством, на
котором устроен Диск:

| Проба | Итог |
|---|---|
| адрес Google при прокси в среде и заведённом исключении | пошёл **напрямую**, прокси не поминается |
| адрес не из исключения (github.com) | пошёл **через прокси**, как и раньше |

То есть исключение делает ровно то, что обещано: Google — напрямую, всё
остальное — как было.

## Что делать владельцу — одна строка

**Запустите приложение Google Диска (Пуск → Google Drive); если спросит вход —
войдите.** Закрывать не нужно: оно уже закрыто, я это проверил — ни одного его
процесса не идёт.

Если после запуска Диск всё же не оживёт в течение нескольких минут — надёжнее
всего перезагрузить компьютер и запустить его ещё раз: работающие программы
узнают о новых переменных только при своём запуске, а иногда — только после
перезагрузки.

## Что уже ждёт выгрузки

В папке Диска на машине лежат и ждут: копия станка `STANOK_20260805-1335`
(сложена и проверена сегодня) и два пробных файла. Как только Диск подключится,
им положено уехать наверх.

**Слежение уже стоит:** запуск Диска я увижу сам, подожду две минуты на
подключение и проверю два признака — перестал ли прокси отвечать отказом 407 и
появились ли папка с копией и пробы в облаке. Итог — «долетело» или «не
долетело» — доложу отдельной короткой отметкой, без ожидания нового задания.
Если не долетит — переходим к rclone, как назначено.

## Дефекты

Дефектов нет. Обнаружено попутно и учтено: на момент правки Диск уже был
полностью закрыт владельцем, поэтому «закрыть и запустить» сжалось до
«запустить».

## Мои ошибки

В этом поручении ошибок не нашёл. Оговорку одну сделаю: проверка механизма шла
на curl вне Диска; сам Диск устроен на нём же, но его собственное чтение
исключения подтвердится только его запуском — это и есть последний шаг,
который остался.

## Чего нельзя утверждать

* **Что Диск оживёт с первого запуска.** Механизм доказан на curl; Диск на нём
  устроен, но проверка делом впереди.
* **Что войти в учётную запись получится без затруднений.** Вход шёл раньше
  через тот же отказ прокси; теперь адреса входа в исключении, но проверит это
  только сам вход.
* **Что перезагрузка не понадобится.** Обычно хватает перезапуска приложения;
  запас назван выше.
